mcpServers:
  jira-ai-field-auto:
    command: node
    args:
      - src/mcp-server.js
    env:
      JIRA_BASE_URL: ${JIRA_BASE_URL}
      JIRA_EMAIL: ${JIRA_EMAIL}
      JIRA_API_TOKEN: ${JIRA_API_TOKEN}
      ANALYSIS_CONCURRENCY: "5"
      DEFAULT_STATUS_FILTER: "未着手"
